import {
  StyleSheet,
} from 'react-native';

export const globalStyles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#0C365A',
  },

  logo: {
    position: 'absolute',
    right: 0,
    marginTop: 36,
    marginRight: 25,
  },
});
